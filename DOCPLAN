Step 1:
User connects when page loads

Step 2: 
on page load onconnection within function
send a message with a type handshake
and a value of the users username

Step 3: 
when and only when this is done, set a state variable of isReady to true

onConnection first hits an if statement, 
if type is handshake then continue 

Step 4: 
server hits if statement swhich is true
server assigns username to specific client

server sends message to switch statement for global or private 
message handler 









a message gets published to kafka
kafka subscribers listen
for each published message. check message group id
on join, set username to clients as hashmap
set hashmap to groups hashmap like groups[groupId] = clients
